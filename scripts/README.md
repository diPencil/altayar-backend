# Scripts Directory

هذا المجلد يحتوي على جميع السكربتات المؤقتة والأدوات المساعدة للتطوير والاختبار.

## أنواع السكربتات:

### 🔧 Scripts للإعداد والتهيئة:
- `init_db.py`, `setup_database.py`, `setup_memberships.py` - إعداد قاعدة البيانات
- `create_*.py` - إنشاء بيانات تجريبية
- `seed_*.py` - ملء قاعدة البيانات بالبيانات الأولية

### 🐛 Scripts للتصحيح والاختبار:
- `debug_*.py` - سكربتات تصحيح الأخطاء
- `test_*.py` - سكربتات الاختبار
- `check_*.py` - سكربتات التحقق من البيانات

### 🔄 Scripts للتحديث والإصلاح:
- `fix_*.py` - إصلاح مشاكل في قاعدة البيانات
- `update_*.py` - تحديث البيانات والإعدادات
- `patch_*.py` - patches لقاعدة البيانات

### ✅ Scripts للتحقق:
- `verify_*.py` - التحقق من صحة البيانات والعمليات

## ملاحظات مهمة:
- هذه السكربتات **مؤقتة** وتستخدم أثناء التطوير فقط
- **لا تستخدم في الإنتاج**
- يمكن حذف هذا المجلد بعد الانتهاء من التطوير




