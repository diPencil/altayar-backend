# Database Settings
# For VPS using SQLite, use a relative path like this:
DATABASE_URL=sqlite:///./altayarvip.db

# JWT Configuration
# CRITICAL: These must be unique and secure for production!
JWT_SECRET_KEY=PLACEHOLDER_FOR_SECURE_RANDOM_STRING
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440
JWT_REFRESH_TOKEN_EXPIRE_DAYS=30

# Security Settings
# CRITICAL: This must be set for production!
SECRET_KEY=PLACEHOLDER_FOR_SECURE_RANDOM_STRING
BACKEND_CORS_ORIGINS=["*"]

# Fawaterk Payment Gateway
FAWATERK_API_KEY=your_real_api_key
FAWATERK_VENDOR_KEY=your_real_vendor_key
FAWATERK_PROVIDER_KEY=your_real_provider_key
FAWATERK_BASE_URL=https://app.fawaterk.com/api/v2
FAWATERK_TEST_MODE=false
# 2=Fawry (recommended if Card fails with 422), 1=Card
FAWATERK_DEFAULT_PAYMENT_METHOD=2
# Optional: override currency sent to Fawaterk (USD, EGP, etc.). Leave empty to use order/booking currency.
# FAWATERK_FORCE_CURRENCY=

# Application URLs (production) - required for payment redirects (Fawaterk requires HTTPS)
APP_BASE_URL=https://api.altayarvip.sbs
# Use this if you only need payment redirects; backend will build success/fail URLs from it
PAYMENT_REDIRECT_BASE_URL=https://api.altayarvip.sbs
# Optional: set explicitly. If unset, backend derives from APP_BASE_URL or PAYMENT_REDIRECT_BASE_URL
# PAYMENT_SUCCESS_URL=https://api.altayarvip.sbs/api/payments/success
# PAYMENT_FAIL_URL=https://api.altayarvip.sbs/api/payments/fail

# Application Settings
APP_NAME=AltayarVIP
APP_VERSION=1.0.0
API_V1_PREFIX=/api
DEBUG=false

# Email Settings (Optional)
EMAILS_FROM_EMAIL=noreply@altayarvip.com
EMAILS_FROM_NAME=AltayarVIP

# Currency & Localization
DEFAULT_CURRENCY=USD
SUPPORTED_CURRENCIES=["USD","EUR","SAR","EGP"]
DEFAULT_CURRENCY_USD_ENABLED=false
DEFAULT_LANGUAGE=ar
SUPPORTED_LANGUAGES=["ar","en"]

# Tax & Pagination
DEFAULT_TAX_RATE=14.0
DEFAULT_PAGE_SIZE=20
MAX_PAGE_SIZE=100
